apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Runnable example: `kubectl apply -k kubernetes/`
# Edit configmap.yaml (policy) and secret.yaml (MaxMind creds) for your deployment.
namespace: geoip-authz

resources:
  - namespace.yaml
  - configmap.yaml
  - blocklist-configmap.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml
  - servicemonitor.yaml
